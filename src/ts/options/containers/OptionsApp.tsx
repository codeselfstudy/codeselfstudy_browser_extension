import * as React from "react";
import styled from "styled-components";

// TODO this isn't loading on the options screen. I didn't look closely at it yet.
export default function OptionsApp() {
    return (
        <>
            <OptionsAppContainer>
                <h1>Options coming soon</h1>
            </OptionsAppContainer>
        </>
    );
}

const OptionsAppContainer = styled("div")`

`;
